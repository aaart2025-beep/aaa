export default function Loading() {
  return (
    <main className="book-theme bg-grid-paper flex min-h-screen items-center justify-center">
      <p className="font-typewriter animate-pulse text-[11px] uppercase tracking-[0.3em] text-ink/70">
        inking the page…
      </p>
    </main>
  );
}
